-- Nothing to undo. The rows cleared here held a format that was never read from
-- any file, so there is no earlier value worth restoring; re-stamping them would
-- only put the false origin back.
SELECT 1;
